<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <!-- background -->
            <gradient android:startColor="@color/light_orange_bottom"
                android:endColor="@color/light_orange_top"
                android:angle="90"/>
            <!-- corner roundness -->
            <corners android:radius="20dp" />
        </shape>
    </item>
</selector>